[project]
name = "enhancer-sdk"
version = "0.1.0"
description = "Sandbox SDK for Enhancer generated code"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.3.4"]
